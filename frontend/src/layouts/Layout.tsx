import Header from "../components/Header";

const Layout = () => {
    return (
        <div className="flex flex-col h-screen">
            <Header />
        </div>
    );
};

export default Layout;